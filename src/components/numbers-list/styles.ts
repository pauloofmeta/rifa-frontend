import styled from "styled-components";

export const NumContent = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 0.5rem;
  justify-content: center;
  padding-bottom: 5rem;
`